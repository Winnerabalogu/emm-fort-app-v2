// declarations.d.ts

/**
 * This declaration tells TypeScript that a module named 'phpass' exists,
 * even though it doesn't have its own built-in types.
 * It allows us to import from it without getting a type error.
 */
declare module 'phpass';